import { ToolComponent } from './ToolComponent'

export class BGPRouteServerComponent extends ToolComponent {
  render(): string {
    // This is handled by the React component
    return ''
  }

  getStyles(): string {
    // Styles are handled by the CSS file
    return ''
  }

  attachEventListeners(): void {
    // Event handling is done in the React component
  }
}